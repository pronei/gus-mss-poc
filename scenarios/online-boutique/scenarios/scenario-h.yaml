name: "Scenario H: Safe upgrade (frontend only)"
description: "Frontend v2 upgrades alone; shipping stays at v1 accepting [standard, express]; no breaks"
baseline:
  frontend: v1
  shipping: v1
upgrades:
  frontend: v2
expect:
  gus: PASS
  breaks_exact: true
