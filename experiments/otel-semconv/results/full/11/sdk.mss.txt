=== GUS Check: [T11-sdk] 1.25.0 -> 1.26.0: upgrade sdk ===
Decision: NO

Chain db.client/db.instance.id — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "db.client/db.instance.id" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain db.client/db.namespace — BREAK (chain-field-missing):
  sdk -> backend via sdk -> collector -> backend
  field "db.namespace" not found at intermediate collector (chain broken — declare x-alias at the renaming hop)
  attributed within the batch to: sdk

Chain db.client/db.operation.name — BREAK (chain-field-missing):
  sdk -> backend via sdk -> collector -> backend
  field "db.operation.name" not found at intermediate collector (chain broken — declare x-alias at the renaming hop)
  attributed within the batch to: sdk

Chain db.client/db.query.text — BREAK (chain-field-missing):
  sdk -> backend via sdk -> collector -> backend
  field "db.query.text" not found at intermediate collector (chain broken — declare x-alias at the renaming hop)
  attributed within the batch to: sdk

Chain db.client/db.user — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "db.client/db.user" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.client.id — BREAK (chain-field-missing):
  sdk -> backend via sdk -> collector -> backend
  field "messaging.client.id" not found at intermediate collector (chain broken — declare x-alias at the renaming hop)
  attributed within the batch to: sdk

Chain messaging/messaging.operation.type — BREAK (chain-field-missing):
  sdk -> backend via sdk -> collector -> backend
  field "messaging.operation.type" not found at intermediate collector (chain broken — declare x-alias at the renaming hop)
  attributed within the batch to: sdk

--- MSS Result ---
Safe subset: {} (empty — no upgrades can deploy safely)

Excluded:
  sdk v1.25.0→v1.26.0 — breaks data-flow chain "db.client/db.instance.id" (chain-no-provider)

Post-hoc verification of the safe subset: PASS
