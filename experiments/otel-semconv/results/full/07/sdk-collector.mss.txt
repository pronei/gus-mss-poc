=== GUS Check: [T07-sdk-collector] 1.22.0 -> 1.23.0: upgrade sdk+collector ===
Decision: NO

Chain http.client/http.request.body.size — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.client/http.request.body.size" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain http.client/http.resend_count — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.client/http.resend_count" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain http.client/http.response.body.size — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.client/http.response.body.size" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain http.client/network.type — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.client/network.type" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain http.server/http.request.body.size — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.server/http.request.body.size" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain http.server/http.response.body.size — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.server/http.response.body.size" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain http.server/network.type — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.server/network.type" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

--- MSS Result ---
Safe subset: {collector v1.22.0→v1.23.0}

Excluded:
  sdk v1.22.0→v1.23.0 — breaks data-flow chain "http.client/http.request.body.size" (chain-no-provider)

Post-hoc verification of the safe subset: PASS
