=== GUS Check: [T16-sdk] 1.30.0 -> 1.31.0: upgrade sdk ===
Decision: NO

Chain messaging/error.type — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/error.type" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.batch.message_count — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.batch.message_count" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.client.id — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.client.id" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.consumer.group.name — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.consumer.group.name" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.destination.anonymous — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.destination.anonymous" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.destination.name — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.destination.name" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.destination.partition.id — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.destination.partition.id" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.destination.subscription.name — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.destination.subscription.name" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.destination.template — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.destination.template" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.destination.temporary — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.destination.temporary" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.message.body.size — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.message.body.size" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.message.conversation_id — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.message.conversation_id" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.message.envelope.size — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.message.envelope.size" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.message.id — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.message.id" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.operation.name — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.operation.name" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.operation.type — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.operation.type" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/messaging.system — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.system" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/network.peer.address — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/network.peer.address" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/network.peer.port — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/network.peer.port" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/server.address — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/server.address" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

Chain messaging/server.port — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/server.port" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: sdk

--- MSS Result ---
Safe subset: {} (empty — no upgrades can deploy safely)

Excluded:
  sdk v1.30.0→v1.31.0 — breaks data-flow chain "messaging/error.type" (chain-no-provider)

Post-hoc verification of the safe subset: PASS
