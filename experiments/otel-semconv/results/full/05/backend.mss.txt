=== GUS Check: [T05-backend] 1.20.0 -> 1.21.0: upgrade backend ===
Decision: NO

Chain container/container.command — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "container/container.command" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain container/container.command_args — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "container/container.command_args" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain container/container.command_line — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "container/container.command_line" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain container/container.image.id — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "container/container.image.id" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain db.client/network.transport — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "db.client/network.transport" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain db.client/network.type — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "db.client/network.type" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain db.client/server.address — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "db.client/server.address" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain db.client/server.port — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "db.client/server.port" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain db.client/server.socket.address — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "db.client/server.socket.address" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain db.client/server.socket.port — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "db.client/server.socket.port" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain faas/cloud.resource_id — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "faas/cloud.resource_id" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain http.client/http.request.method_original — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.client/http.request.method_original" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain http.client/network.transport — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.client/network.transport" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain http.client/network.type — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.client/network.type" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain http.client/server.address — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.client/server.address" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain http.client/server.port — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.client/server.port" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain http.client/server.socket.address — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.client/server.socket.address" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain http.client/server.socket.port — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.client/server.socket.port" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain http.server/client.port — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.server/client.port" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain http.server/client.socket.address — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.server/client.socket.address" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain http.server/client.socket.port — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.server/client.socket.port" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain http.server/http.request.method_original — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.server/http.request.method_original" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain http.server/network.transport — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.server/network.transport" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain http.server/network.type — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.server/network.type" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain http.server/url.path — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.server/url.path" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain http.server/url.query — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "http.server/url.query" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain k8s.cluster/k8s.cluster.uid — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "k8s.cluster/k8s.cluster.uid" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain messaging/messaging.client.id — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.client.id" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain messaging/messaging.destination.anonymous — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.destination.anonymous" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain messaging/messaging.destination.name — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.destination.name" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain messaging/messaging.destination.template — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.destination.template" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain messaging/messaging.destination.temporary — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/messaging.destination.temporary" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain messaging/network.transport — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/network.transport" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain messaging/network.type — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/network.type" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain messaging/server.address — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/server.address" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain messaging/server.socket.address — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/server.socket.address" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain messaging/server.socket.port — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "messaging/server.socket.port" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.client/network.transport — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.client/network.transport" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.client/network.type — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.client/network.type" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.client/rpc.method — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.client/rpc.method" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.client/rpc.service — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.client/rpc.service" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.client/rpc.system.name — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.client/rpc.system.name" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.client/server.address — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.client/server.address" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.client/server.port — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.client/server.port" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.client/server.socket.address — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.client/server.socket.address" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.client/server.socket.domain — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.client/server.socket.domain" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.client/server.socket.port — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.client/server.socket.port" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.server/client.address — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.server/client.address" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.server/client.port — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.server/client.port" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.server/client.socket.address — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.server/client.socket.address" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.server/client.socket.port — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.server/client.socket.port" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.server/network.transport — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.server/network.transport" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.server/network.type — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.server/network.type" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain rpc.server/server.port — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "rpc.server/server.port" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

Chain service/service.version — BREAK (chain-no-provider):
  (no provider) -> backend
  nothing in the mesh provides identity "service/service.version" required by backend (declare x-provides at its source, or the field was withdrawn)
  attributed within the batch to: backend

--- MSS Result ---
Safe subset: {} (empty — no upgrades can deploy safely)

Excluded:
  backend v1.20.0→v1.21.0 — breaks data-flow chain "container/container.command" (chain-no-provider)

Post-hoc verification of the safe subset: PASS
